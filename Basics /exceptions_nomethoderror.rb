# It occurs when there is no such methods defined 

puts "hello".len # NO method error occurs because the method is length not len 

puts 5.len  #No method error occurs because integer do not have method len  